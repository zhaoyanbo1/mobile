# 数据源配置
spring:
  flyway:
    enabled: false
    locations: classpath:db/sqlite
    baselineOnMigrate: true
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: org.sqlite.JDBC
    druid:
      url: jdbc:sqlite:${SQLITE_PATH:demo.sqlite}?journal_mode=WAL&busy_timeout=5000
      username:
      password:

